# Proyecto-Web-Ejemplo
El proyecto consistirá en crear una pequeña aplicación web donde Python (con un micro‑framework como Flask) se encargará de la lógica del servidor, y HTML mostrará la información al usuario en el navegador. El servidor en Python recibirá peticiones, procesará datos o ejecutará funciones, y luego enviará una página HTML como respuesta. 
